export type UserRole = 'seeker' | 'employer';
export type Language = 'EN' | 'FR';

export interface Localization {
  title: string;
  subtitle: string;
  emailLabel: string;
  passLabel: string;
  forgotPass: string;
  actionBtn: string;
  footerText: string;
  footerLink: string;
}
